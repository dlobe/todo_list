// app.js
function addTask() {
  // Get the task input value
  var task = document.getElementById("task").value;
  // Send the task to the server
  // (We will implement this in the Application Tier)
}
